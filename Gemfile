source 'https://rubygems.org'

gemspec

# NOTE: As a result of the gemspec, bundler reports duplicates:
# Your Gemfile lists the gem caxlsx_rails (>= 0) more than once.
# Your Gemfile lists the gem sqlite3 (~> 1.4) more than once.
# Your Gemfile lists the gem paranoia (>= 0) more than once.
# You should probably keep only one of them.
# Remove any duplicate entries and specify the gem only once (per group).
# - 
# I believe the gemspec should take precedence. [nimmolo 2022-05-31]

# gem 'rails'
# gem 'terminal-table'
# gem 'simple_form', '>= 5.0.1'
# gem 'ransack', '>= 2.3.2'
# gem 'kaminari', '>= 0.17.0'

# for Rails 5+
# gem 'rubyzip', '>= 1.2.1'
# gem 'caxlsx' # For some reason, doesn't load from gemspec in Rails 6
# gem 'caxlsx_rails' # For some reason, doesn't load from gemspec in Rails 6

# group :assets do
#   gem 'uglifier' # Needs to be in group :assets
# end

# group :test do
  # gem "sqlite3", '~> 1.4'
# end

# gem 'will_paginate'

# gem 'paranoia'
